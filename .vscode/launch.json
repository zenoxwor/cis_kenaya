{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open browser (localhost:8000)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:8000",
      "webRoot": "${workspaceFolder}"
    }
  ]
}